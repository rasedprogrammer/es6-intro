const add = (first, second) => first + second;
const multiply = (first, second) => first + second;
const addAll = (a, b, c, d, e, f, g) => a + b + c + d + e + f + g;
// No Parameters Arrow Functions
const getPie = () => 3.14;
// One Parameters Functions
const doubled = (num) => num * 5;
//One Parameters Functions Simple Version
const simpleArrow = (num) => num * 5;
//Multiple arrow functions
const multipleLine = (x, y, x) => {
	const firstMultiply = x + y;
	const secondMultiply = y + z;
	const multiply = firstMultiply * secondMultiply;
	const result = multiply / 2;
	return result;
};

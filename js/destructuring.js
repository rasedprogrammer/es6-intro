const man = {
	name: "rased",
	age: 26,
	profission: "job",
	salary: 16000,
};
const { name, age, profission, salary } = man;

// Array
const numbers = [2, 4, 6, 7, 3, 6, 1];
const [one, two, three, four, five, six, seven] = numbers;
